import 'package:flutter/material.dart';

class Hamstringsscreen extends StatelessWidget {
  const Hamstringsscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}